<!-- impeccable:product-schema 1 -->

# Selfcraft 产品事实

## 它是什么

Selfcraft 是一个持续存在于本地环境中的个人智能体。它能够在长期相处中形成身份、关系、记忆与能力，并在发现自身问题后提出改进，经验证后更新自己的 Runtime。

系统只有一个 Runtime。CLI、Web 以及未来的其他渠道只是同一个智能体的不同沟通入口，不拥有彼此独立的记忆、身份或 Agent Loop。

## 为谁服务

首版面向希望在自己的设备或私有环境中长期运行个人助理的单个用户。用户可以理解基础的模型渠道配置，但不应为了使用助理而理解会话、上下文压缩或内部记忆结构。

## 用户首先要完成的事

- 自然地开始或延续一段对话
- 看见助理当前是在思考、使用工具还是等待
- 在同一处配置模型渠道、活动模型与时区
- 确认 Runtime 是否健康、数据存放在哪里
- 从 CLI 或 Web 进入时仍面对同一个助理
- 在任一通信入口完成首次认识，不依赖另一入口代为初始化

## 产品原则

- 对话是主入口，配置与状态服务于对话，不把产品做成运维面板
- 系统持续存在，但不要求用户理解或管理 session
- 记忆、任务和工具状态由服务端持久化，浏览器不是事实来源
- 默认实例没有预设姓名和人格，它们应在相处中逐渐形成
- 界面语言自然、克制、具体，不使用夸张承诺或机器人式流程话术
- 密钥只写入本地受限的凭证文件，任何读取接口都不得返回明文
- Onboarding 是各入口对同一初始化状态的表达，不属于进程启动器；CLI 使用终端交互，Web 使用对话式引导
- 首次认识只收集模型与时区，第一段对话必须是真实 Agent 回应；姓名、人格和关系留给长期相处

## 首版范围

首版 Web 提供首次认识、持续对话、流式状态、历史消息、浅色/深色主题，以及模型与时区配置。公网托管、多人账号、跨设备同步、远程鉴权和完整的记忆管理界面不在本次范围内。
